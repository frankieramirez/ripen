# Issue tracker: <name>

Tracker: other
Project: <what the user called it>
Adapter flags: none. The skills follow the Conventions below.
Auth: <the environment variable or login the user named, never a value>
Keys: <what a ticket identifier looks like>

## Conventions

<The user's paragraph, tidied into one sentence per operation: how to create a ticket, read one, list open ones by label or state, apply and remove a label or state, assign someone, comment, and close. Name the tool or API for each. When an operation has no way to happen here, say so, and the skills report it instead of pretending.>

## Pull requests as a request surface

No.

## External authors

<who counts>

## Wayfinding operations

<How a map and its tickets are represented, how blocking is recorded, what claiming means, and what resolving writes where. Leave this as "not supported" if the user does not want maps here; the map-walking skill then keeps its maps under .scratch/.>
